{
    "buildCommand": "cd client && npm install && npm run build",
    "outputDirectory": "build",
    "framework": "vite",
    "rewrites": [
      {
        "source": "/API(.*)",
        "destination": "/API/server.js"
      }
    ]
}